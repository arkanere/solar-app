/**
 * Site chrome. `Chrome` is what a layout mounts; the header and footer are
 * exported too because the specimen sheets render them on their own.
 */
export { Chrome } from './Chrome';
export { SiteHeader } from './SiteHeader';
export { SiteFooter } from './SiteFooter';
